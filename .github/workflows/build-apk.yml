name: Build Android Artifacts

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK packages
        run: yes | sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK and AAB
        run: gradle --no-daemon assembleDebug bundleDebug

      - name: Verify APK
        run: test -f app/build/outputs/apk/debug/app-debug.apk

      - name: Verify AAB
        run: test -f app/build/outputs/bundle/debug/app-debug.aab

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: PrivateExpenseTracker-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Upload AAB for Google Play Internal App Sharing
        uses: actions/upload-artifact@v4
        with:
          name: PrivateExpenseTracker-debug-aab
          path: app/build/outputs/bundle/debug/app-debug.aab
          if-no-files-found: error
